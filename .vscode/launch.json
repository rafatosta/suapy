{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Executar script principal",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/run.py",
            "console": "integratedTerminal"
        }
    ]
}
